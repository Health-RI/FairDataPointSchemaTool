services:
  schema-tool:
    build:
      context: .
    network_mode: host
    volumes:
      - ./${PROP_LOCATION}:/opt/app/Properties.yaml:ro
    command: ["-h", "${FDP_HOST}", "-u", "${FDP_USER}", "-p", "${FDP_PWD}"]
